import { BgcDirective } from './bgc.directive';

describe('BgcDirective', () => {
  it('should create an instance', () => {
    const directive = new BgcDirective();
    expect(directive).toBeTruthy();
  });
});
